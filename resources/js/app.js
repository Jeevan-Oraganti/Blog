import './bootstrap';
import Vue from 'vue';
import ExampleComponent from './ExampleComponent.vue';

new Vue({
    el: "#app",
    data: {
        data: data,
    },
    components: {
        ExampleComponent,
    },
});
